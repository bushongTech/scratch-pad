# IP address of the DAQ device — actively used
DeviceIP: '192.168.10.25'

# Transmit port from translator to DAQ — actively used
DevicePortTx: 29099

# Receive port from DAQ to translator — actively used
DevicePortRx: 24242

# Source name used in message headers — actively used
Source: "MainControl"

# Kafka bootstrap server (not used by translator_udp.py; connection comes from message_broker_config.yaml via common.MessageBroker)
BootstrapServer: '192.168.10.14:9093'  # unused